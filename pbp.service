# /etc/systemd/system/pbp.service

[Unit]
Description=gunicorn daemon
Requires=pbp.socket
After=network.target


[Service]
User=root
Group=www-data
WorkingDirectory=/home/gonzalo/pbp
ExecStart=/home/gonzalo/pbp/.venv/bin/gunicorn     --access-logfile -     --workers 3     --bind unix:/run/pbp.sock     main.wsgi:application

[Install]
WantedBy=multi-user.target
